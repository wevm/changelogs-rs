name: Release

on:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: Version
    if: github.repository == 'tempoxyz/changelogs' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Install Rust
        uses: tempoxyz/gh-actions/vendor/dtolnay/rust-toolchain@6f436eb3a168279692739e8c0c9fdb52b94bfae7

      - name: Install changelogs
        run: cargo install --path .

      - name: Fetch GitHub token via STS
        id: app-token
        uses: tempoxyz/gh-actions/actions/github-sts@c0292122e255def866c64c55e5844d1e7d5fe7ad
        with:
          policy: release

      - name: Changelogs
        id: changelogs
        uses: ./
        with:
          crate-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          conventional-commit: true
          github-token: ${{ steps.app-token.outputs.token }}

      - name: Build release binaries
        if: steps.changelogs.outputs.published == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          for tag in $(git tag --points-at HEAD); do
            gh workflow run binary.yml \
              --ref "$GITHUB_REF_NAME" \
              --field tag="$tag"
          done
