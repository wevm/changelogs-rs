---
changelogs: minor
---

Added AI-assisted changelog generation via `--ai` flag. The AI command can be specified inline (`--ai "amp -x"`) or configured in `.changelog/config.toml` under `[ai]` section.
